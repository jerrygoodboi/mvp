{
  "name": "qr-auth-backend",
  "version": "1.0.0",
  "description": "Node.js Express backend for QR-based passwordless authentication",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build-client": "cd client && npm install && npm run build",
    "install-all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.5",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "jsonwebtoken": "^9.0.0",
    "qrcode": "^1.5.0",
    "uuid": "^9.0.0",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
